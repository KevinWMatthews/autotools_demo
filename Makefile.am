# List subdirectories that Automake should process.
# These are procesed recursively but in the order given.
# The current directory '.' is implicitly listed last!
SUBDIRS = common include libjupiter src
